services:
  solver:
    build: .
    image: tents-sat:latest
    container_name: tents-sat-solver
    command: ["./resolutionTents", "Tests/Grille1_66.txt"]

  shell:
    build: .
    image: tents-sat:latest
    container_name: tents-sat-shell
    command: ["bash"]
    stdin_open: true
    tty: true
